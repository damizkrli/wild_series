{{ form_start(form) }}
    {{ form_widget(form) }}
<div class="container">
    <div class="row p-5 justify-content-center">
        <button class="btn btn-outline-dark m-5">{{ button_label|default('Save') }}</button>
    </div>
</div>

{{ form_end(form) }}
